import flushDatabase from '../utils/flush-database';

describe('find bidding processes and items test', () => {
  beforeEach(async () => {});

  afterEach(async () => {
    await flushDatabase();
  });

  it('should return a paginated processes and your items', async () => {});

  it('should return a processes and your items filtered by biddingStartDatetime', async () => {});

  it('should return a processes and your items filtered by number of process', async () => {});

  it('should return a processes and your items filtered by summary', async () => {});

  it('should return a processes and your items filtered by description of item', async () => {});

  it('should return a second page of paginated processes and your items', async () => {});
});
